// Write a function that takes an array of numbers and returns the first number greater than 10 using the find method.
const arr = [5, 8, 55, 0, 12, 9];
const newarr = arr.find((num) => num > 10);
console.log(newarr);
