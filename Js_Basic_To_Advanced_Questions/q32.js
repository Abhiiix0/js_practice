// Write a function that takes a string and replaces all occurrences of "apple" with "orange".
const str = " this is apple string to test apple";
function replaceStr() {
  return str.replaceAll("apple", "orange");
}
console.log(replaceStr(str));
