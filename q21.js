// Write a function that takes a number and returns "Even" if it's even and "Odd" if it's odd using the ternary operator.
// ternary operator in js
function EvenOrOdd(num) {
  return num % 2 === 0 ? "Even" : "Odd";
}
console.log(EvenOrOdd(44));
